const router = require('express').Router()
// GET /api/map/locations  — location entities with coordinates for Leaflet
// GET /api/map/entries    — entries tagged with location entities
module.exports = router
